<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/login_button"
        android:layout_below="@+id/dialoc_title"
        android:background="@color/colorPrimary"
        android:textColor="#FFFFFF"
        android:layout_marginTop="80dp"
        android:text="Login"
        android:textAllCaps="false"
        android:textSize="20sp"
        android:padding="20dp"
        android:layout_alignLeft="@+id/sign_up_button"
        android:layout_alignStart="@+id/sign_up_button"
        android:layout_alignRight="@+id/sign_up_button"
        android:layout_alignEnd="@+id/sign_up_button" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/sign_up_button"
        android:layout_below="@+id/login_button"
        android:background="@color/colorPrimary"
        android:textColor="#FFFFFF"
        android:layout_marginTop="20dp"
        android:text="Sign Up"
        android:textAllCaps="false"
        android:textSize="20sp"
        android:padding="20dp"
        android:layout_alignLeft="@+id/skip_sign_up_button"
        android:layout_alignStart="@+id/skip_sign_up_button"
        android:layout_alignRight="@+id/skip_sign_up_button"
        android:layout_alignEnd="@+id/skip_sign_up_button" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/skip_sign_up_button"
        android:background="@color/colorPrimary"
        android:textColor="#FFFFFF"
        android:layout_marginTop="22dp"
        android:text="Skip Sign Up"
        android:textAllCaps="false"
        android:textSize="20sp"
        android:padding="20dp"
        android:layout_below="@+id/sign_up_button"
        android:layout_centerHorizontal="true" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/dialoc_title"
        android:text="Dialoc"
        android:textAllCaps="false"
        android:textSize="50sp"
        android:textColor="@color/colorPrimary"
        android:layout_marginTop="66dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />

</RelativeLayout>